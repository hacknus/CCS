# Copy this file to .devcontainer/.env and replace both password values.
# .devcontainer/.env is ignored by Git.
MYSQL_DATABASE=mib_schema_coca
MYSQL_USER=ccs
MYSQL_PASSWORD=replace-with-a-local-development-password
MYSQL_ROOT_PASSWORD=replace-with-a-different-root-password
